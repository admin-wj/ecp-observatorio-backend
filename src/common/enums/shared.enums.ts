export enum Polarity {
  negative = 'Negativo',
  neutral = 'Neutro',
  positive = 'Positivo',
}

export enum LocationDataKeys {
  AFFINITY = 'affinity',
  RISK = 'risk_index',
  IMPACT = 'impact_index',
}